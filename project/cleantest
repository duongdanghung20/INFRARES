#!/bin/bash -x
# enlever les switches
BRIDGES=$(cat test|awk '/^ovs-vsctl add-br/ { print $3 }')
NAMESPACES=$(cat test| awk '/^ip netns add/ { print $4 }')
LINKS=$(cat test|awk '/peer name/ { print $9 }')
DIRECTORIES=$(cat test|awk '/mkdir / { print $3 }')
for BRIDGE in $BRIDGES
do
	sudo ovs-vsctl del-br $BRIDGE
done
for NAMESPACE in $NAMESPACES
do
	sudo ip netns del $NAMESPACE
done
for LINK in $LINKS
do
	sudo ip l del $LINK
done
for DIRECTORY in $DIRECTORIES
do
	sudo rm $DIRECTORY/*
done
